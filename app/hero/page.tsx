import HeroDemo from "@/components/HeroDemo";

export const metadata = {
    title: "Hero Showcase | DocuForge",
    description: "A stunning visual demo with WebGL shaders and liquid buttons.",
};

export default function HeroPage() {
    return <HeroDemo />;
}
